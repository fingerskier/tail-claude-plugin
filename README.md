# tail-claude-plugin
Runs an app in a sub-process and stream logs back to Claude 

* run an app in a sub-process
* stream logs back to Claude
* tail N logs
* kill the process
* restart the process
* list running processes
